;;; Code
(require 'package)
(add-to-list 'package-archives
  '("melpa" . "http://melpa.milkbox.net/packages/") t)
(add-to-list 'package-archives '(("ELPA" . "http://tromey.com/elpa/") 
                          ("gnu" . "http://elpa.gnu.org/packages/")
                          ("marmalade" . "http://marmalade-repo.org/packages/"))) 
(package-initialize)
 
;make sure the packages are installed; if not install them
(mapc
 (lambda (package)
   (or (package-installed-p package)
       (package-install package)
   )
   )
 '
										; generated by C-h v package-activated-list  
( ac-clang  ac-helm  ac-html  alert  anaconda-mode  async  auctex  auto-complete  auto-complete-clang  autopair  biblio  biblio-core  bind-key  cl-generic  company  concurrent  counsel  counsel-projectile  ctable  cuda-mode  dash  deferred  diminish  direx  ecb  ein  elpy  epc  epl  es-lib  exec-path-from-shell  f  find-file-in-project  flx  flycheck  git-commit  gntp  helm  helm-bibtex  helm-core  highlight-indentation  hydra  ivy  jedi  jedi-core  jedi-direx  js2-mode  json-rpc  key-chord  let-alist  log4e  magit  magit-popup  markdown-mode  markdown-mode+  matlab-mode  mode-icons  multiple-cursors  org  org-dotemacs  org-mobile-sync  org-ref  org-wc  org-wunderlist  parsebib  pdf-tools  phi-rectangle  php-mode  pkg-info  popup  popup-complete  pos-tip  powerline  pretty-lambdada  projectile  py-autopep8  py-isort  python-django  python-environment  pyvenv  rainbow-mode  request  request-deferred  s  scss-mode  seq  simple-httpd  skewer-mode  smex  swiper  tablist  tidy  toc-org  undo-tree  use-package  wc-mode  websocket  with-editor  yasnippet  )

);; close the package automanagement function

(defun set-exec-path-from-shell-PATH ()
  "Sets the exec-path to the same value used by the user shell"
  (let ((path-from-shell
         (replace-regexp-in-string
          "[[:space:]\n]*$" ""
          (shell-command-to-string "$SHELL -l -c 'echo $PATH'"))))
    (setenv "PATH" path-from-shell)
    (setq exec-path (split-string path-from-shell path-separator))))
 
;; call function now
(unless (string-equal system-type "windows-nt") ; Microsoft Windows
    (set-exec-path-from-shell-PATH))
(provide 'my_package_management)
;;; my_package_management.el ends here
